"""API surface discovery (ROADMAP.md §2b).

Spec discovery, GraphQL introspection, passive HAR capture, spec synthesis, and
action-to-endpoint correlation. Reports "everything observed so far", never a
guaranteed-exhaustive census — see §2b's bounded claim.
"""
