"""Client-side signals catalog (ROADMAP.md §2c).

Storage state, console/errors, accessibility tree, response headers (free/near-
free defaults), plus opt-in cheaper and expensive signals. Anything storage- or
CPU-heavy is gated behind an explicit flag, never part of a default run.
"""
